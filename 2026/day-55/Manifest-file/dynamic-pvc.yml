kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: dynamic-pvc

spec:
  resources:
    requests:
      storage: 10Gi

  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
