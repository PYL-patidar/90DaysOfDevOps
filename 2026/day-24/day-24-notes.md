```
Q1) Observe the merge — did Git do a fast-forward merge or a merge commit?
ANS : if changes of other branch adding to the main branch then fast-forwarding merge done but , in the case where changes are present in both branches that means
braches are diversing so that in this situation merge commit are done. 







```
